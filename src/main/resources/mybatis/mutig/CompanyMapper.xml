<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.project.multig.mapper.CompanyMapper">

    <!-- 其他查询操作 ... -->

    <!-- 根据任务ID查询相关联的企业 -->
    <select id="selectCompaniesByTaskId" resultType="com.ruoyi.project.multig.domain.Company">
        SELECT c.company_id AS "id", c.company_name_sxz AS "name"
        FROM company_name_sxz c
                 JOIN evaluate_info_sxz tcl ON c.company_id = tcl.company_info_sxz
        WHERE tcl.task_info= #{taskId}
    </select>


</mapper>
