<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.project.multig.mapper.ScoreRecordMapper">

    <!-- 查询所有任务 -->
    <insert id="insertScoreRecord" parameterType="com.ruoyi.project.multig.domain.ScoreRecord">
        INSERT INTO score_records_sxz (task_id, evaluated_company_id, score, timestamp) VALUES (#{scoreRecord.taskId}, #{scoreRecord.evaluatedCompanyId}, #{scoreRecord.score}, #{scoreRecord.timestamp})
    </insert>
    <select id="selectScoreHistory" resultType="com.ruoyi.project.multig.domain.ScoreRecord">
        SELECT * FROM score_records_sxz ORDER BY timestamp DESC
    </select>
</mapper>

